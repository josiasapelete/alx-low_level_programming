#include "main.h"
#include <string.h>
/*
 * _strlen - returns the lenghts of a string
 * @s: the strin
 * Return: The length of @s
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
