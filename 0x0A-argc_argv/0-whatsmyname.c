#include "main.h"

/**
 * main - function that prints its name
 * @argc: the number of the arguments
 * @argv: array that contain all the name of argument
 *
 * Return: always return 0
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
